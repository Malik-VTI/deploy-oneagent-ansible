---
# Connection settings for Linux targets (SSH + privilege escalation).
ansible_connection: ssh
ansible_user: ansible          # CHANGE ME: SSH user that has sudo
ansible_become: true                   # OneAgent install needs root
# ansible_ssh_private_key_file: ~/.ssh/id_rsa
