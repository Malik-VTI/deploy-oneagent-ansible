---
# Inventory of hosts that will receive the Dynatrace OneAgent.
# Group by OS so the correct connection settings apply (see group_vars/).
all:
  children:
    linux:
      hosts:
        linux-host-01:
          ansible_host: 10.0.0.11
        # linux-host-02:
        #   ansible_host: 10.0.0.12

    windows:
      hosts:
        # windows-host-01:
        #   ansible_host: 10.0.0.21
